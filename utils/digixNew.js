export const DigixNew = (message) => {

    si (!message) retourner null ;

    const content = message.viewOnceMessageV2?.message || message;
    
    renvoyer le contenu ;
}

exporter par défaut DigixNew;