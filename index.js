import connectToWhatsapp from './Digix/crew.js'
import handleIncomingMessage from './events/messageHandler.js'

(async() => {
    attendre connectToWhatsapp(gérerMessageEntrant)
        console.log('établi !')
})(